/**
 * ============================================================
 * クレーム(ご意見)AI自動分類システム 【統合版・1ファイル】
 * ------------------------------------------------------------
 * この1ファイルだけをApps Scriptに貼り付ければ動きます。
 * （src/ の Config.gs + Anonymize.gs + Setup.gs + Code.gs を結合）
 * ============================================================
 */

/* ==================== 1/4 Config ==================== */
/**
 * Config.gs
 * ------------------------------------------------------------------
 * クレーム(ご意見)AI自動分類システム 設定・マスターデータ定義
 *
 * ★本番シート「ご意見記録」(A〜CI列/87列)の実列構成に合わせた設計です。
 *   既存データを壊さないよう、システムが触れる列を厳密に限定しています。
 *
 * このファイルは「変更してよい値」を集約しています。
 * ロジックは Setup.gs / Anonymize.gs / Code.gs 側にあります。
 * ------------------------------------------------------------------
 */

/** シート名（表記揺れ防止のため必ずこの定数を参照する） */
const SHEETS = {
  CLAIM:        'ご意見記録',              // ★メイン表。名称が異なる場合はここを変更
  RULE_DAI:     '仕訳ルールマスター_大分類',
  RULE_CHU:     '仕訳ルールマスター_中分類',
  RULE_SHO:     '仕訳ルールマスター_小分類',
  SCENE_M:      '発生場面マスター',
  SCENE_RULE:   '発生場面ルールマスター',
  CAUSE_M:      '原因分類マスター',
  CAUSE_RULE:   '原因分類ルールマスター',
  DAI_LIST:     '大分類一覧',
  CONFIG:       '設定',
  ACCURACY:     '精度検証',
  STORE:        '店舗マスタ'   // ★ユーザー管理（システムは読むだけ・作成/上書きしない）
};

/**
 * ★店番→店舗情報の自動補完設定。
 *  受付表(ご意見記録)の「店番」に入力すると、店舗マスタから下記の見出しの値を
 *  受付表の同名見出し列へ"値で"書き込む（記録として凍結）。
 *  受付表・店舗マスタの両方に、下記の見出し名が存在すればそのまま動く（列の並びは不問）。
 */
const STORE_KEY_HEADER = '店番'; // 突合キー（受付表・店舗マスタ共通の見出し）
const STORE_FILL_HEADERS = ['店名', '営業統括部', '営業部', 'ブロック', 'エリア'];

/**
 * AI関数名。環境により =AI() / =Gemini()。ここだけ差し替える。
 */
const AI_FUNCTION_NAME = 'AI';

/**
 * マスター参照の上限行数（開放範囲 A2:A を避け現実的な上限で固定）。
 */
const LIMITS = {
  DAI:   30,   // 大分類ルール（現11種＋追加余地。増やす時はここを拡張）
  CHU:   200,  // 中分類マスター
  SHO:   400,  // 小分類マスター
  SCENE: 60,   // 発生場面
  CAUSE: 60,   // 原因分類
  ACC:   2000, // 精度検証で集計するメイン表の上限行
  TEMPLATE_ROWS: 500 // 原文が無くても補助列にテンプレ数式を入れておく行数
};

/** AI関数(=AI(単一セル))を入れる列キー。空行には入れない（AI("")防止）。 */
const AI_COLS = ['AI_SUM','AI_DAI','AI_CHU','AI_SHO','AI_REASON','AI_SCENE','AI_CAUSE'];

/* ============================================================
 * ★メイン表「ご意見記録」の列位置（実構成に準拠）
 *   数値=列番号(1始まり)、CL=数式で使う列レター。
 * ============================================================ */
const COL = {
  // PII（触れない。匿名化はAB→ADで実施）
  NAME: 14,  // N 氏名
  PHONE:15,  // O 電話番号
  MAIL: 16,  // P メールアドレス
  // 内容
  RAW:    28, // AB 内容（原文）      ★触れない
  AI_SUM: 29, // AC 内容（要約）      ← =AI(要約用結合文CL)  AI要約(200字/顧客目線)
  AITEXT: 30, // AD AI投入用テキスト  ← =ANONYMIZE(AB)
  // AI出力
  AI_DAI:   31, // AE AI大分類
  AI_CHU:   32, // AF AI中分類
  AI_SHO:   33, // AG AI小分類
  AI_SCENE: 34, // AH AI発生場面
  AI_CAUSE: 35, // AI AI原因分類
  AI_REASON:36, // AJ AI理由
  // 確定（人が選ぶ。触れない＝プルダウンのみ設定）
  FIX_DAI:  37, // AK 大分類
  FIX_CHU:  38, // AL 中分類
  FIX_SHO:  39, // AM 小分類
  FIX_SCENE:40, // AN 発生場面
  FIX_CAUSE:41, // AO 原因分類
  // 補助（結合文）
  G_DAI:  42, // AP 大分類用結合文
  I_CHU:  43, // AQ 中分類候補文
  J_CHU:  44, // AR 中分類用結合文
  L_REASON:45,// AS 補助セル（AI理由用）
  P_SCENE:46, // AT 発生場面候補文（結合文兼用）
  R_CAUSE:47, // AU 原因分類候補文（結合文兼用）
  // ★追加：小分類の補助列（末尾 CJ/CK に新設）
  V_SHO:  88, // CJ 小分類候補文
  W_SHO:  89, // CK 小分類用結合文
  SUM_TXT:90  // CL 要約用結合文（AC=AI要約 の入力プロンプト）★新設
};

/** 数式内で使う列レター */
const CL = {
  RAW:'AB', AI_SUM:'AC', AITEXT:'AD',
  AI_DAI:'AE', AI_CHU:'AF', AI_SHO:'AG', AI_SCENE:'AH', AI_CAUSE:'AI', AI_REASON:'AJ',
  FIX_DAI:'AK', FIX_CHU:'AL', FIX_SHO:'AM', FIX_SCENE:'AN', FIX_CAUSE:'AO',
  G_DAI:'AP', I_CHU:'AQ', J_CHU:'AR', L_REASON:'AS', P_SCENE:'AT', R_CAUSE:'AU',
  V_SHO:'CJ', W_SHO:'CK', SUM_TXT:'CL'
};

/**
 * ★列位置を「見出し名」で自動解決するための対応表（キー → 1行目の見出しテキスト）。
 * 列を並び替えても、この見出しさえ一致していれば正しい列を自動で見つけます。
 * 見出しを改名した場合は、ここのテキストも合わせて更新してください。
 * （上の COL / CL は、見出しが見つからないとき用のフォールバック既定位置）
 */
const HEADER_TEXT = {
  RAW:      '内容（原文）',
  AI_SUM:   '内容（要約）',
  AITEXT:   'AI投入用テキスト',
  AI_DAI:   'AI大分類',
  AI_CHU:   'AI中分類',
  AI_SHO:   'AI小分類',
  AI_SCENE: 'AI発生場面',
  AI_CAUSE: 'AI原因分類',
  AI_REASON:'AI理由',
  FIX_DAI:  '大分類',
  FIX_CHU:  '中分類',
  FIX_SHO:  '小分類',
  FIX_SCENE:'発生場面',
  FIX_CAUSE:'原因分類',
  G_DAI:    '大分類用結合文',
  I_CHU:    '中分類候補文',
  J_CHU:    '中分類用結合文',
  L_REASON: '補助セル',
  P_SCENE:  '発生場面候補文',
  R_CAUSE:  '原因分類候補文',
  V_SHO:    '小分類候補文',    // 無ければ自動新設
  W_SHO:    '小分類用結合文',  // 無ければ自動新設
  SUM_TXT:  '要約用結合文'     // 無ければ自動新設
};

/** 見出しが無ければ末尾に新設してよいシステム補助列（それ以外は必須＝無ければ警告） */
const SYSTEM_HELPER_KEYS = ['V_SHO', 'W_SHO', 'SUM_TXT'];

/** 精度検証などで使う参考列（必須ではない。見出しで探し、無ければ既定レターにフォールバック） */
const REF_HEADER_TEXT = { STORE: '店名' };
const REF_FALLBACK_LETTER = { STORE: 'W' };

/**
 * システムが「数式を書き込む」列（＝上書きされる列）。この列以外は触れない。
 * 原文AB・確定AK〜AO・PII・社員情報などは対象外。
 * ※AC(要約)はAI要約を入れるため対象に含む（既存の手入力要約があれば上書きされる点に注意）。
 */
const FORMULA_TARGET_COLS = [
  'SUM_TXT','AI_SUM','AITEXT','G_DAI','AI_DAI','I_CHU','J_CHU','AI_CHU',
  'V_SHO','W_SHO','AI_SHO','L_REASON','AI_REASON',
  'P_SCENE','AI_SCENE','R_CAUSE','AI_CAUSE'
];

/** 設定シートに書き出すキー・値 */
const CONFIG_ROWS = [
  ['キー', '値', '説明'],
  ['メイン表シート名', SHEETS.CLAIM, '数式・プルダウンを設定する対象シート'],
  ['AI関数名', AI_FUNCTION_NAME, '=AI() か =Gemini() か。Config.gs の AI_FUNCTION_NAME と揃える'],
  ['最終セットアップ日時', '', 'setup 実行時に自動記録']
];

/* ==== 大分類ルールマスター（列: 大分類/定義/入る例/しない例/着眼点） ==== */
const MASTER_DAI = [
  ['大分類','定義','この分類に入る例','この分類にしない例','判定の着眼点'],
  ['レジ接客','レジでの接客・会計対応（態度／マニュアル／知識／医薬品）が主因','レジでの態度／会計ミス／ポイント・クーポン対応／レジでの医薬品対応','売場での対応（→外周り接客）／価格の不一致（→アンマッチ）','不満の起点がレジ担当者の対応か'],
  ['外周り接客','売場・外周りでの接客・作業対応（態度／売場作業／商品／医薬品）が主因','売場での態度／電話・返品対応／在庫問い合わせ／売場での医薬品対応','レジでの対応（→レジ接客）','不満の起点が売場・外周り担当者の対応か'],
  ['アンマッチ','表示（棚札・ポイント）とレジ結果の不一致が主因','棚とレジで売価が違う／ポイントが合わない','欠品（→欠品・特注）','表示とレジ結果の不一致が起点か'],
  ['欠品・特注','商品の欠品・特注に関するご意見','定番／チラシ商品の欠品／特注対応','品揃えの要望（→商品・扱い品）','欲しい商品が無い／特注が起点か'],
  ['商品・扱い品','商品そのものの品質・制限・期限・品揃えが主因','製品不良／点数制限／期限切れ／品揃え','欠品（→欠品・特注）','商品自体に関する不満か'],
  ['クリンリネス','店内外の清掃・衛生状態が主因','店内／テスター／店外・駐車場／トイレ','従業員の身だしなみ（→接客態度）','清掃・衛生状態が起点か'],
  ['アプリ','自社アプリの仕組み・内容が主因','アプリの使い勝手・仕様・内容','店員のアプリ知識不足（→レジ接客:知識）','アプリ自体が起点か'],
  ['販促関連','キャンペーン・企画・販促の内容やシステムが主因','企画内容／キャンペーンのシステム','店員の販促対応（→接客）','販促企画・仕組みそのものが起点か'],
  ['EC','ネット通販・店舗受け取りに関するご意見','EC関連／店舗受け取り','店頭の特注対応（→外周り接客）','EC・受け取りが起点か'],
  ['その他','設備・閉店・その他（騒音・配送など）','設備（店内／本部）／閉店／その他','明確に他分類に入るもの','上記いずれにも当てはまらないか'],
  ['事件・事故','店内外の事件・事故に関するご意見','事件／事故','通常のクレーム・ご意見','事件・事故性があるか'],
  ['問い合わせ・要望','商品・サービス・店舗等への問い合わせや要望','各種問い合わせ／改善要望','明確なクレーム（該当分類へ）','問い合わせ・要望が主か'],
  ['営業','営業時間・営業体制など営業に関する事項','営業時間／営業体制への意見','店頭接客（→接客）','営業に関する事項か'],
  ['社内','社内向け・内部連絡に関する事項','社内連絡／内部管理に関する事項','顧客対応（該当分類へ）','社内事項か'],
  ['人事書類','人事・書類に関する事項','人事関連／書類手続き','顧客対応（該当分類へ）','人事・書類事項か'],
  ['テナント接客','テナントの接客に関する事項','テナント従業員の接客','自店の接客（→レジ/外周り接客）','テナントの接客か'],
  ['テナント商品','テナントの商品に関する事項','テナントの商品・品揃え','自店の商品（→商品・扱い品）','テナントの商品か'],
  ['お褒め','お褒め・感謝の声','良かった対応への感謝','クレーム・要望（該当分類へ）','肯定的な声か']
];

/* ==== 中分類マスター（列: 大分類/中分類/定義/入る例/しない例） ==== */
const MASTER_CHU = [
  ['大分類','中分類','定義','この中分類に入る例','この中分類にしない例'],
  ['レジ接客','接客態度（レジ接客）','大分類「レジ接客」における「接客態度（レジ接客）」に関するご意見','表情（レジ接客）／口調・発言（レジ接客）／挨拶（レジ接客）／動作（レジ接客）／態度（レジ接客）／私語（レジ接客）／配慮（レジ接客）／身だしなみ（レジ接客）',''],
  ['レジ接客','マニュアル','大分類「レジ接客」における「マニュアル」に関するご意見','レジ並び・誘導・レジ混雑／ポイント不備・聞かない／クーポン対応不備／キャンペーン対応不備／会計ミス／サッカー関連／商品・物の扱い',''],
  ['レジ接客','知識','大分類「レジ接客」における「知識」に関するご意見','アプリ・キャンペーン・ポイントの知識／クーポン・決済の知識',''],
  ['レジ接客','薬（レジ接客）','大分類「レジ接客」における「薬（レジ接客）」に関するご意見','医薬品対応・販売方法',''],
  ['外周り接客','接客態度（外周り接客）','大分類「外周り接客」における「接客態度（外周り接客）」に関するご意見','表情（外周り接客）／口調・発言（外周り接客）／挨拶（外周り接客）／動作（外周り接客）／態度（外周り接客）／私語（外周り接客）／配慮（外周り接客）／身だしなみ（外周り接客）',''],
  ['外周り接客','売場・作業時','大分類「外周り接客」における「売場・作業時」に関するご意見','電話対応／返品・交換対応／在庫確認・問い合わせ対応／特注対応・ＥＣ店舗受け取り／棚・通路を開けない/お客様の近くで作業／開店・閉店作業／万引き扱い／商品の取り扱い方、音を立てて作業',''],
  ['外周り接客','商品','大分類「外周り接客」における「商品」に関するご意見','商品知識／点数制限対応',''],
  ['外周り接客','薬（外周り接客）','大分類「外周り接客」における「薬（外周り接客）」に関するご意見','医薬品対応・販売方法／薬剤師不在／医薬品アラート',''],
  ['アンマッチ','売価アンマッチ','大分類「アンマッチ」における「売価アンマッチ」に関するご意見','売価アンマッチ',''],
  ['アンマッチ','ポイントアンマッチ','大分類「アンマッチ」における「ポイントアンマッチ」に関するご意見','ポイントアンマッチ',''],
  ['欠品・特注','欠品','大分類「欠品・特注」における「欠品」に関するご意見','定番／チラシ',''],
  ['欠品・特注','特注','大分類「欠品・特注」における「特注」に関するご意見','特注',''],
  ['商品・扱い品','製品不良','大分類「商品・扱い品」における「製品不良」に関するご意見','製品不良',''],
  ['商品・扱い品','点数制限','大分類「商品・扱い品」における「点数制限」に関するご意見','点数制限',''],
  ['商品・扱い品','期限切れ','大分類「商品・扱い品」における「期限切れ」に関するご意見','期限切れ',''],
  ['商品・扱い品','品揃え','大分類「商品・扱い品」における「品揃え」に関するご意見','品揃え',''],
  ['クリンリネス','クリンリネス','大分類「クリンリネス」における「クリンリネス」に関するご意見','店内／テスター／店外・駐車場／トイレ',''],
  ['アプリ','アプリ','大分類「アプリ」における「アプリ」に関するご意見','アプリ',''],
  ['販促関連','販促','大分類「販促関連」における「販促」に関するご意見','企画内容／システム（キャンペーンのシステム）',''],
  ['EC','EC','大分類「EC」における「EC」に関するご意見','EC関連／店舗受け取り',''],
  ['その他','設備','大分類「その他」における「設備」に関するご意見','店内／本部',''],
  ['その他','閉店','大分類「その他」における「閉店」に関するご意見','閉店',''],
  ['その他','その他','大分類「その他」における「その他」に関するご意見','その他（騒音・配送など含む）／その他',''],
  ['事件・事故','事件・事故','大分類「事件・事故」における「事件・事故」に関するご意見','事件・事故',''],
  ['問い合わせ・要望','問い合わせ・要望','大分類「問い合わせ・要望」における「問い合わせ・要望」に関するご意見','問い合わせ・要望',''],
  ['営業','営業','大分類「営業」における「営業」に関するご意見','営業',''],
  ['社内','社内','大分類「社内」における「社内」に関するご意見','社内',''],
  ['人事書類','人事書類','大分類「人事書類」における「人事書類」に関するご意見','人事書類',''],
  ['テナント接客','テナント接客','大分類「テナント接客」における「テナント接客」に関するご意見','テナント接客',''],
  ['テナント商品','テナント商品','大分類「テナント商品」における「テナント商品」に関するご意見','テナント商品',''],
  ['お褒め','お褒め','大分類「お褒め」における「お褒め」に関するご意見','お褒め','']
];

/* ==== 小分類マスター（列: 大分類/中分類/小分類/定義/入る例/しない例） ==== */
const MASTER_SHO = [
  ['大分類','中分類','小分類','定義','この小分類に入る例','この小分類にしない例'],
  ['レジ接客','接客態度（レジ接客）','表情（レジ接客）','中分類「接客態度（レジ接客）」のうち「表情（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','口調・発言（レジ接客）','中分類「接客態度（レジ接客）」のうち「口調・発言（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','挨拶（レジ接客）','中分類「接客態度（レジ接客）」のうち「挨拶（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','動作（レジ接客）','中分類「接客態度（レジ接客）」のうち「動作（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','態度（レジ接客）','中分類「接客態度（レジ接客）」のうち「態度（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','私語（レジ接客）','中分類「接客態度（レジ接客）」のうち「私語（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','配慮（レジ接客）','中分類「接客態度（レジ接客）」のうち「配慮（レジ接客）」に該当するもの','',''],
  ['レジ接客','接客態度（レジ接客）','身だしなみ（レジ接客）','中分類「接客態度（レジ接客）」のうち「身だしなみ（レジ接客）」に該当するもの','',''],
  ['レジ接客','マニュアル','レジ並び・誘導・レジ混雑','中分類「マニュアル」のうち「レジ並び・誘導・レジ混雑」に該当するもの','',''],
  ['レジ接客','マニュアル','ポイント不備・聞かない','中分類「マニュアル」のうち「ポイント不備・聞かない」に該当するもの','',''],
  ['レジ接客','マニュアル','クーポン対応不備','中分類「マニュアル」のうち「クーポン対応不備」に該当するもの','',''],
  ['レジ接客','マニュアル','キャンペーン対応不備','中分類「マニュアル」のうち「キャンペーン対応不備」に該当するもの','',''],
  ['レジ接客','マニュアル','会計ミス','中分類「マニュアル」のうち「会計ミス」に該当するもの','',''],
  ['レジ接客','マニュアル','サッカー関連','中分類「マニュアル」のうち「サッカー関連」に該当するもの','',''],
  ['レジ接客','マニュアル','商品・物の扱い','中分類「マニュアル」のうち「商品・物の扱い」に該当するもの','',''],
  ['レジ接客','知識','アプリ・キャンペーン・ポイントの知識','中分類「知識」のうち「アプリ・キャンペーン・ポイントの知識」に該当するもの','',''],
  ['レジ接客','知識','クーポン・決済の知識','中分類「知識」のうち「クーポン・決済の知識」に該当するもの','',''],
  ['レジ接客','薬（レジ接客）','医薬品対応・販売方法','中分類「薬（レジ接客）」のうち「医薬品対応・販売方法」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','表情（外周り接客）','中分類「接客態度（外周り接客）」のうち「表情（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','口調・発言（外周り接客）','中分類「接客態度（外周り接客）」のうち「口調・発言（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','挨拶（外周り接客）','中分類「接客態度（外周り接客）」のうち「挨拶（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','動作（外周り接客）','中分類「接客態度（外周り接客）」のうち「動作（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','態度（外周り接客）','中分類「接客態度（外周り接客）」のうち「態度（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','私語（外周り接客）','中分類「接客態度（外周り接客）」のうち「私語（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','配慮（外周り接客）','中分類「接客態度（外周り接客）」のうち「配慮（外周り接客）」に該当するもの','',''],
  ['外周り接客','接客態度（外周り接客）','身だしなみ（外周り接客）','中分類「接客態度（外周り接客）」のうち「身だしなみ（外周り接客）」に該当するもの','',''],
  ['外周り接客','売場・作業時','電話対応','中分類「売場・作業時」のうち「電話対応」に該当するもの','',''],
  ['外周り接客','売場・作業時','返品・交換対応','中分類「売場・作業時」のうち「返品・交換対応」に該当するもの','',''],
  ['外周り接客','売場・作業時','在庫確認・問い合わせ対応','中分類「売場・作業時」のうち「在庫確認・問い合わせ対応」に該当するもの','',''],
  ['外周り接客','売場・作業時','特注対応・ＥＣ店舗受け取り','中分類「売場・作業時」のうち「特注対応・ＥＣ店舗受け取り」に該当するもの','',''],
  ['外周り接客','売場・作業時','棚・通路を開けない/お客様の近くで作業','中分類「売場・作業時」のうち「棚・通路を開けない/お客様の近くで作業」に該当するもの','',''],
  ['外周り接客','売場・作業時','開店・閉店作業','中分類「売場・作業時」のうち「開店・閉店作業」に該当するもの','',''],
  ['外周り接客','売場・作業時','万引き扱い','中分類「売場・作業時」のうち「万引き扱い」に該当するもの','',''],
  ['外周り接客','売場・作業時','商品の取り扱い方、音を立てて作業','中分類「売場・作業時」のうち「商品の取り扱い方、音を立てて作業」に該当するもの','',''],
  ['外周り接客','商品','商品知識','中分類「商品」のうち「商品知識」に該当するもの','',''],
  ['外周り接客','商品','点数制限対応','中分類「商品」のうち「点数制限対応」に該当するもの','',''],
  ['外周り接客','薬（外周り接客）','医薬品対応・販売方法','中分類「薬（外周り接客）」のうち「医薬品対応・販売方法」に該当するもの','',''],
  ['外周り接客','薬（外周り接客）','薬剤師不在','中分類「薬（外周り接客）」のうち「薬剤師不在」に該当するもの','',''],
  ['外周り接客','薬（外周り接客）','医薬品アラート','中分類「薬（外周り接客）」のうち「医薬品アラート」に該当するもの','',''],
  ['アンマッチ','売価アンマッチ','売価アンマッチ','中分類「売価アンマッチ」のうち「売価アンマッチ」に該当するもの','',''],
  ['アンマッチ','ポイントアンマッチ','ポイントアンマッチ','中分類「ポイントアンマッチ」のうち「ポイントアンマッチ」に該当するもの','',''],
  ['欠品・特注','欠品','定番','中分類「欠品」のうち「定番」に該当するもの','',''],
  ['欠品・特注','欠品','チラシ','中分類「欠品」のうち「チラシ」に該当するもの','',''],
  ['欠品・特注','特注','特注','中分類「特注」のうち「特注」に該当するもの','',''],
  ['商品・扱い品','製品不良','製品不良','中分類「製品不良」のうち「製品不良」に該当するもの','',''],
  ['商品・扱い品','点数制限','点数制限','中分類「点数制限」のうち「点数制限」に該当するもの','',''],
  ['商品・扱い品','期限切れ','期限切れ','中分類「期限切れ」のうち「期限切れ」に該当するもの','',''],
  ['商品・扱い品','品揃え','品揃え','中分類「品揃え」のうち「品揃え」に該当するもの','',''],
  ['クリンリネス','クリンリネス','店内','中分類「クリンリネス」のうち「店内」に該当するもの','',''],
  ['クリンリネス','クリンリネス','テスター','中分類「クリンリネス」のうち「テスター」に該当するもの','',''],
  ['クリンリネス','クリンリネス','店外・駐車場','中分類「クリンリネス」のうち「店外・駐車場」に該当するもの','',''],
  ['クリンリネス','クリンリネス','トイレ','中分類「クリンリネス」のうち「トイレ」に該当するもの','',''],
  ['アプリ','アプリ','アプリ','中分類「アプリ」のうち「アプリ」に該当するもの','',''],
  ['販促関連','販促','企画内容','中分類「販促」のうち「企画内容」に該当するもの','',''],
  ['販促関連','販促','システム（キャンペーンのシステム）','中分類「販促」のうち「システム（キャンペーンのシステム）」に該当するもの','',''],
  ['EC','EC','EC関連','中分類「EC」のうち「EC関連」に該当するもの','',''],
  ['EC','EC','店舗受け取り','中分類「EC」のうち「店舗受け取り」に該当するもの','',''],
  ['その他','設備','店内','中分類「設備」のうち「店内」に該当するもの','',''],
  ['その他','設備','本部','中分類「設備」のうち「本部」に該当するもの','',''],
  ['その他','閉店','閉店','中分類「閉店」のうち「閉店」に該当するもの','',''],
  ['その他','その他','その他（騒音・配送など含む）','中分類「その他」のうち「その他（騒音・配送など含む）」に該当するもの','',''],
  ['その他','その他','その他','中分類「その他」のうち「その他」に該当するもの','',''],
  ['事件・事故','事件・事故','事件・事故','中分類「事件・事故」のうち「事件・事故」に該当するもの','',''],
  ['問い合わせ・要望','問い合わせ・要望','問い合わせ・要望','中分類「問い合わせ・要望」のうち「問い合わせ・要望」に該当するもの','',''],
  ['営業','営業','営業','中分類「営業」のうち「営業」に該当するもの','',''],
  ['社内','社内','社内','中分類「社内」のうち「社内」に該当するもの','',''],
  ['人事書類','人事書類','人事書類','中分類「人事書類」のうち「人事書類」に該当するもの','',''],
  ['テナント接客','テナント接客','テナント接客','中分類「テナント接客」のうち「テナント接客」に該当するもの','',''],
  ['テナント商品','テナント商品','テナント商品','中分類「テナント商品」のうち「テナント商品」に該当するもの','',''],
  ['お褒め','お褒め','お褒め','中分類「お褒め」のうち「お褒め」に該当するもの','','']
];

/* ================================================================
 * 発生場面マスター（※実データに定義が無いため既定値。運用で調整可）
 * 列: 分類名 / 定義 / この分類に入る例 / この分類にしない例
 * ================================================================ */
const MASTER_SCENE = [
  ['分類名','定義','この分類に入る例','この分類にしない例'],
  ['来店前','来店前（チラシ・電話・アプリ確認等）の場面','来店前の情報・予約','店内での出来事'],
  ['入店時','入店直後の場面','入店時の挨拶がない','売場回遊（→売場回遊時）'],
  ['売場回遊時','売場を見て回っている場面','回遊中の出来事','商品選択（→商品確認・選択時）'],
  ['商品確認・選択時','商品を確認・選択する場面','商品を選ぶ時の不満','欠品確認（→欠品・在庫確認時）'],
  ['売場での接客・案内時','売場で従業員が接客・案内する場面','売場での接客・案内','レジ（→レジ会計時）'],
  ['欠品・在庫確認時','欠品や在庫を確認する場面','在庫問い合わせ/欠品','商品選択（→商品確認・選択時）'],
  ['特注・EC受取対応時','特注やEC店舗受取の場面','特注/店舗受取対応','通常接客（→売場での接客・案内時）'],
  ['レジ待ち時','レジに並んで待つ場面','レジ待ちが長い','会計処理（→レジ会計時）'],
  ['レジ会計時','レジで会計する場面','会計ミス/売価不一致/ポイント','セルフレジ（→セルフレジ利用時）'],
  ['セルフレジ利用時','セルフレジ利用の場面','セルフレジ操作・決済','有人レジ（→レジ会計時）'],
  ['ポイント・クーポン利用時','ポイント・クーポン利用の場面','ポイント/クーポンが使えない','会計処理（→レジ会計時）'],
  ['医薬品対応時','医薬品の相談・販売の場面','医薬品対応/薬剤師','通常商品（各場面）'],
  ['電話対応時','電話でのやり取りの場面','電話応対の不満','来店時対応'],
  ['返品・交換対応時','返品・交換の対応場面','返品可否のやり取り','通常問い合わせ（→売場での接客・案内時）'],
  ['作業対応時','品出し等の作業中の場面','作業中の対応/作業音','接客（各場面）'],
  ['退店時','退店時の場面','退店時の対応','駐車場（→駐車場・店外施設利用時）'],
  ['駐車場・店外施設利用時','駐車場・店外施設利用の場面','駐車場が使いにくい','店内（各場面）'],
  ['アプリ・EC利用時','アプリ・ECサイト利用の場面','アプリ/ECの利用','店員の知識（→売場/レジ）'],
  ['不明','発生場面が特定できない','場面が判断できない','場面が明確なもの']
];

const MASTER_SCENE_RULE = [
  ['判定ルール'],
  ['発生場面は「お客様の不満が最初に強くなった業務場面」で判定する。'],
  ['セルフレジはセルフレジ利用時、有人レジはレジ会計時とする。'],
  ['欠品や在庫の確認は欠品・在庫確認時とする。'],
  ['返品・交換の具体的やり取りは返品・交換対応時とする。'],
  ['場面が特定できない場合のみ不明とする。']
];

/* ================================================================
 * 原因分類マスター（※実データに定義が無いため既定値。運用で調整可）
 * ================================================================ */
const MASTER_CAUSE = [
  ['分類名','定義','この分類に入る例','この分類にしない例'],
  ['教育不足','従業員教育・トレーニング不足','接客教育がされていない','個人の知識不足（→知識不足）'],
  ['知識不足','担当者の商品・業務知識不足','商品/アプリを説明できない','確認を怠った（→確認不足）'],
  ['確認不足','必要な確認を怠った','在庫/価格/ポイントを確認しない','知識自体がない（→知識不足）'],
  ['ルール未整備','ルール・基準が存在しない','対応基準のルールがない','ルールが伝わっていない（→ルール周知不足）'],
  ['ルール周知不足','ルールはあるが周知されていない','現場に伝わっていない','ルール自体がない（→ルール未整備）'],
  ['引継ぎ不備','担当間の引継ぎ不備','引き継がれていない','人員不足（→人員不足）'],
  ['人員不足','人員そのものが不足','人が足りず回らない','忙しくて漏れた（→多忙による対応漏れ）'],
  ['多忙による対応漏れ','繁忙で対応が漏れた','忙しくて対応漏れ','人が足りない（→人員不足）'],
  ['応援体制不備','繁忙時の応援体制不備','応援が来ない','責任者不在（→責任者不在）'],
  ['責任者不在','責任者・管理者が不在','責任者がいない','部門連携（→部門間連携不足）'],
  ['部門間連携不足','部門間の連携不足','部門間で話が通らない','業務分担不明（→業務分担不明確）'],
  ['業務分担不明確','業務分担・担当が不明確','誰の担当か不明','引継ぎ（→引継ぎ不備）'],
  ['システム不備','システム・画面設計の不備','アプリ/POSが使いにくい','機器故障（→機器不備）'],
  ['機器不備','機器・端末の不具合','端末が故障','システム設計（→システム不備）'],
  ['設備不備','店舗設備の不備','設備が壊れている','機器（→機器不備）'],
  ['商品起因','商品そのものに起因','商品の品質・仕様','取引先起因（→取引先起因）'],
  ['取引先起因','取引先・仕入先に起因','仕入先の問題','自社の商品管理（→商品起因）'],
  ['表示設計不備','値札・POP・表示の設計不備','表示が紛らわしい','システム画面（→システム不備）'],
  ['売場設計不備','売場・棚割の設計不備','売場が分かりにくい','表示（→表示設計不備）'],
  ['案内不足','必要な案内が不足','案内が足りない','説明が足りない（→説明不足）'],
  ['説明不足','必要な説明が不足','説明が足りない','案内が足りない（→案内不足）'],
  ['判断基準不統一','担当・場面で基準が異なる','担当により回答が違う','ルール周知不足（→ルール周知不足）'],
  ['顧客との認識差','顧客と店舗の認識のずれ','思い込みとのずれ','店舗側の明確な不備（各原因）'],
  ['原因不明','背景要因が特定できない','原因が判断できない','原因が特定できるもの']
];

const MASTER_CAUSE_RULE = [
  ['判定ルール'],
  ['原因分類は表面的な不満ではなく「社内として直すべき背景要因」で判定する。'],
  ['ルールが無い→ルール未整備、あるが伝わっていない→ルール周知不足。'],
  ['人が足りない→人員不足、繁忙で漏れた→多忙による対応漏れ。'],
  ['知識が無く説明できない→知識不足、確認を怠った→確認不足。'],
  ['アプリ/システムが分かりにくい→システム不備。']
];

/* ==================== 2/4 Anonymize ==================== */
/**
 * Anonymize.gs
 * ------------------------------------------------------------------
 * 個人情報の匿名化（推奨案 = GASカスタム関数）
 *
 * 【2案の比較】
 *  案1: REGEXREPLACE を数式だけで多段ネストして匿名化する
 *       - 長所: GAS不要
 *       - 短所: 数式が長大でネストが深くなり「開始括弧が足りない」等の
 *               エラーが出やすく、保守が困難。パターン追加のたびに全行修正。
 *  案2(推奨): GASカスタム関数 =ANONYMIZE(E2) で匿名化する
 *       - 長所: パターンを1か所で管理でき、可読性・保守性が高い。
 *               警告判定も同じ場所で行える。数式はセルに =ANONYMIZE(E2) だけ。
 *       - 短所: スクリプトのインストールが必要（本システムでは元々GAS前提）。
 *
 * → 本システムは案2を推奨・実装。案1相当の数式もREADMEに記載。
 *
 * 【方針】
 *  - 氏名・住所を「過剰に」消して商品名・店舗名・地名・意味まで失わないよう、
 *    ラベル（氏名：/住所： 等）の直後だけをマスクする保守的な設計にする。
 *  - 電話・メール・郵便番号・URL・長い数字列は形式で検出してマスクする。
 * ------------------------------------------------------------------
 */

/**
 * クレーム原文を匿名化して返す。セルには =ANONYMIZE(E2) と入れる。
 * @param {string|string[][]} input 原文（配列範囲にも対応）
 * @return {string|string[][]} 匿名化済みテキスト
 * @customfunction
 */
function ANONYMIZE(input) {
  // 配列（範囲）で渡された場合は各要素を再帰処理
  if (Array.isArray(input)) {
    return input.map(function (row) {
      return row.map(function (v) { return anonymizeOne_(v); });
    });
  }
  return anonymizeOne_(input);
}

/**
 * 1件分の匿名化本体。
 * @param {*} raw
 * @return {string}
 */
function anonymizeOne_(raw) {
  if (raw === null || raw === undefined) return '';
  var s = String(raw);
  if (s.trim() === '') return '';

  // 1) メールアドレス
  s = s.replace(/[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}/g, '[メール]');

  // 2) URL
  s = s.replace(/https?:\/\/[^\s　、。）)]+/g, '[URL]');

  // 3) 郵便番号（〒付き）※〒があるものは電話より先に確定させる
  s = s.replace(/〒\s?\d{3}[-－]\d{4}/g, '[郵便番号]');

  // 4) 電話番号（0で始まる 9〜11桁、ハイフン許容）
  s = s.replace(/0\d{1,4}[-－(（]?\d{1,4}[-－)）]?\d{3,4}/g, '[電話番号]');

  // 5) 郵便番号（〒なしの 123-4567 形式。電話処理後の取りこぼし）
  s = s.replace(/\d{3}[-－]\d{4}/g, '[郵便番号]');

  // 6) 会員番号 / 注文番号 / 受注番号 / レシート番号（ラベル + 数字/英数）
  s = s.replace(/(会員番号|会員No|注文番号|受注番号|オーダー番号|レシート番号|取引番号|伝票番号)\s*[:：]?\s*[A-Za-z0-9\-－]{3,}/g, '$1：[番号]');

  // 7) ラベル付き氏名（氏名：/お名前：/顧客名：/名前：の直後の連続文字）
  //    「様」「さん」まで含めてマスク。過剰削除を避けるため句読点・空白で停止。
  s = s.replace(/(氏名|お名前|お客様名|顧客名|名前)\s*[:：]\s*[^\s、。　]{1,20}(様|さん)?/g, '$1：[氏名]');

  // 8) ラベル付き住所（住所：/ご住所：/お届け先：の直後）
  //    ★過剰削除防止: 空白・句読点で必ず停止する（商品名やクレーム本文を巻き込まない）。
  //    住所に内部空白がある場合は空白まで。取りこぼしは ANONYMIZE_WARN で検知。
  s = s.replace(/(ご住所|住所|お届け先|届け先)\s*[:：]\s*[^、。\n\s　]{1,40}/g, '$1：[住所]');

  // 9) 残った長い数字列（7桁以上）を番号としてマスク（電話等の取りこぼし対策）
  s = s.replace(/\d{7,}/g, '[番号]');

  return s;
}

/**
 * 匿名化しきれなかった可能性を検出して警告文言を返す（任意利用）。
 * セルに =ANONYMIZE_WARN(E2) と入れると使える。問題なければ空文字。
 * @param {string} raw 原文
 * @return {string} 警告（該当なしは空文字）
 * @customfunction
 */
function ANONYMIZE_WARN(raw) {
  if (raw === null || raw === undefined) return '';
  // 一般語（お客様など）とマスク済みプレースホルダを除外してから判定
  var s = String(raw).replace(/お客様|皆様|様々|\[[^\]]+\]/g, '');
  var warns = [];

  // ラベルは無いが氏名らしき「◯◯様/さん」が残っていないか
  if (/[一-龥ぁ-んァ-ヶ]{2,}(様|さん)/.test(s)) warns.push('氏名らしき表現');
  // 匿名化後も 7桁以上の数字が残る（通常はマスク済みだが原文チェック用）
  if (/\d{7,}/.test(s)) warns.push('長い数字列');
  // メール/URL の取りこぼし
  if (/@|https?:\/\//.test(s)) warns.push('メール/URL');

  return warns.length ? '要確認:' + warns.join('・') : '';
}

/* ==================== 3/4 Setup ==================== */
/**
 * Setup.gs
 * ------------------------------------------------------------------
 * シート作成・マスター投入・数式設定・プルダウン設定・精度検証構築。
 *
 * 【安全設計（★本番シート「ご意見記録」対応）】
 *  - メイン表で触れるのは FORMULA_TARGET_COLS の列（数式）と AK〜AO のプルダウンのみ。
 *  - 原文AB・要約AC・確定AK〜AO(値)・PII(N/O/P)・社員/管理列(BB〜CI)は一切上書きしない。
 *  - 小分類の補助列 CJ/CK を末尾に新設（既存列を1つもずらさない）。
 *  - マスター系シートはシステム管理なので毎回最新化（メイン表とは別シート）。
 *  - 二重実行しても壊れない。原文が無い行にはAI数式を入れない（AI("")防止）。
 * ------------------------------------------------------------------
 */

/** 変更内容の事前サマリ */
const SETUP_PLAN = [
  '【マスター系シート（無ければ作成し最新化）】',
  '  仕訳ルールマスター_大分類 / _中分類 / _小分類',
  '  発生場面マスター / 発生場面ルールマスター',
  '  原因分類マスター / 原因分類ルールマスター',
  '  大分類一覧 / 設定 / 精度検証',
  '',
  '【メイン表「' + SHEETS.CLAIM + '」で触れる列だけ】',
  '  数式を設定: AC(要約), AD, AP, AE, AQ, AR, AF, CJ(新), CK(新), AG, AS, AJ, AT, AH, AU, AI, CL(新)',
  '  プルダウン: AK, AL, AM, AN, AO',
  '  新ヘッダ追加: CJ=小分類候補文, CK=小分類用結合文, CL=要約用結合文',
  '',
  '【絶対に触れない列】AB(原文) / AK〜AO(確定の値) / N,O,P(個人情報) / BB〜CI(社員・管理)',
  '  ※AC(内容・要約)はAI要約を入れるため上書き対象（既存の手入力要約がある場合は要注意）'
];

/* ============================ 実行エントリ ============================ */

/**
 * マスター系シートを構築（メイン表には触れない。安全）。
 * ★分類マスターは「無ければ作成、あれば温存」＝手編集した分類は上書きしません。
 *   既定値で作り直したい場合はメニュー「マスターを既定値で再作成」を使用。
 */
function setupMasters() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  Logger.log(SETUP_PLAN.join('\n'));
  try {
    // 分類定義マスターは非破壊（既存の手編集を温存）
    ensureMasterSheet_(ss, SHEETS.RULE_DAI,   MASTER_DAI);
    ensureMasterSheet_(ss, SHEETS.RULE_CHU,   MASTER_CHU);
    ensureMasterSheet_(ss, SHEETS.RULE_SHO,   MASTER_SHO);
    ensureMasterSheet_(ss, SHEETS.SCENE_M,    MASTER_SCENE);
    ensureMasterSheet_(ss, SHEETS.SCENE_RULE, MASTER_SCENE_RULE);
    ensureMasterSheet_(ss, SHEETS.CAUSE_M,    MASTER_CAUSE);
    ensureMasterSheet_(ss, SHEETS.CAUSE_RULE, MASTER_CAUSE_RULE);
    // 補助シートは常に最新化（利用者の手編集は想定しない）
    writeDaiList_(ss);
    writeConfigSheet_(ss);
    setupAccuracySheet_(ss);
    stampConfig_(ss);
    SpreadsheetApp.getActive().toast('マスター系シートを構築しました（既存の分類は温存）。', 'クレームAI分類', 5);
  } catch (e) {
    Logger.log('setupMasters: エラー ' + e + '\n' + (e.stack || ''));
    SpreadsheetApp.getUi().alert('マスター構築でエラー: ' + e.message);
    throw e;
  }
}

/**
 * 分類マスターを Config.gs の既定値で「強制的に」作り直す（確認ダイアログ付き）。
 * ★手編集した分類はすべて破棄されます。既定値へ戻したいときだけ使用。
 */
function resetMastersToDefault() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var ui = SpreadsheetApp.getUi();
  var res = ui.alert('マスターを既定値で再作成',
    '分類マスター（大/中/小分類・発生場面・原因分類）を Config.gs の既定値で上書きします。\n' +
    '★シート上で手編集した分類はすべて失われます。よろしいですか？',
    ui.ButtonSet.OK_CANCEL);
  if (res !== ui.Button.OK) return;
  try {
    writeMasterSheet_(ss, SHEETS.RULE_DAI,   MASTER_DAI);
    writeMasterSheet_(ss, SHEETS.RULE_CHU,   MASTER_CHU);
    writeMasterSheet_(ss, SHEETS.RULE_SHO,   MASTER_SHO);
    writeMasterSheet_(ss, SHEETS.SCENE_M,    MASTER_SCENE);
    writeMasterSheet_(ss, SHEETS.SCENE_RULE, MASTER_SCENE_RULE);
    writeMasterSheet_(ss, SHEETS.CAUSE_M,    MASTER_CAUSE);
    writeMasterSheet_(ss, SHEETS.CAUSE_RULE, MASTER_CAUSE_RULE);
    writeDaiList_(ss);
    SpreadsheetApp.getActive().toast('マスターを既定値で再作成しました。', 'クレームAI分類', 5);
  } catch (e) {
    Logger.log('resetMastersToDefault: エラー ' + e);
    ui.alert('再作成でエラー: ' + e.message);
    throw e;
  }
}

/**
 * メイン表「ご意見記録」に数式・プルダウンを適用する（確認ダイアログ付き）。
 * マスターが未作成なら先に setupMasters を実行する。
 */
function applyMainSheetFormulas() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var ui = SpreadsheetApp.getUi();
  var sh = ss.getSheetByName(SHEETS.CLAIM);
  if (!sh) {
    var names = ss.getSheets().map(function (s) { return '・' + s.getName(); }).join('\n');
    ui.alert('メイン表「' + SHEETS.CLAIM + '」が見つかりません。\n\n' +
      'Config.gs の SHEETS.CLAIM を、下の実在シート名のどれかに正確に合わせてください' +
      '（空白・全角半角も一致させる）。\n\n【このブック内のシート】\n' + names);
    return;
  }
  var res = ui.alert('メイン表への数式適用',
    '「' + SHEETS.CLAIM + '」の次の列だけを上書きします。\n' +
    '数式: AC(AI要約),AD,AP,AE,AQ,AR,AF,CJ,CK,AG,AS,AJ,AT,AH,AU,AI,CL\n' +
    'プルダウン: AK〜AO\n' +
    '（原文AB・確定値・個人情報・社員/管理列には触れません。\n' +
    '　AC=要約はAIで上書きします＝既存の手入力要約がある場合はご注意）\n\n実行しますか？',
    ui.ButtonSet.OK_CANCEL);
  if (res !== ui.Button.OK) return;

  try {
    if (!ss.getSheetByName(SHEETS.RULE_CHU)) setupMasters(); // マスター未作成なら作る
    var res = resolveColumns_(sh); // ★見出し名から列位置を解決（並び替えに対応）
    setupMainSheet_(sh, res);
    setupValidations_(ss, sh, res);
    SpreadsheetApp.getActive().toast('メイン表に数式・プルダウンを適用しました。', 'クレームAI分類', 5);
  } catch (e) {
    Logger.log('applyMainSheetFormulas: エラー ' + e + '\n' + (e.stack || ''));
    ui.alert('数式適用でエラー: ' + e.message);
    throw e;
  }
}

/* ============================ シート基盤 ============================ */

function ensureSheet_(ss, name) {
  var sh = ss.getSheetByName(name);
  if (!sh) { sh = ss.insertSheet(name); Logger.log('シート作成: ' + name); }
  return sh;
}

/** マスターを強制投入（既存内容をクリアして最新化）。 */
function writeMasterSheet_(ss, name, data) {
  var sh = ensureSheet_(ss, name);
  sh.clearContents();
  sh.getRange(1, 1, data.length, data[0].length).setValues(data);
  sh.setFrozenRows(1);
  sh.getRange(1, 1, 1, data[0].length).setFontWeight('bold').setBackground('#e8eaed');
  sh.autoResizeColumns(1, data[0].length);
  Logger.log('マスター投入: ' + name + '（' + (data.length - 1) + '件）');
}

/** マスターを「無ければ作成／あれば温存」で用意（手編集を壊さない）。 */
function ensureMasterSheet_(ss, name, data) {
  var sh = ss.getSheetByName(name);
  if (sh && getLastDataRow_(sh, 1) >= 2) {   // 既にデータあり → 温存
    Logger.log('マスター温存: ' + name);
    return;
  }
  writeMasterSheet_(ss, name, data);         // 無い/空 → 既定値を投入
}

/** 大分類一覧（参考用）を大分類ルールマスターの現在の内容から再構築。 */
function writeDaiList_(ss) {
  var src = ss.getSheetByName(SHEETS.RULE_DAI);
  var names = null;
  if (src) {
    var last = getLastDataRow_(src, 1);
    if (last >= 2) names = src.getRange(2, 1, last - 1, 1).getValues();
  }
  if (!names || !names.length) names = MASTER_DAI.slice(1).map(function (r) { return [r[0]]; });

  var sh = ensureSheet_(ss, SHEETS.DAI_LIST);
  sh.clearContents();
  sh.getRange(1, 1, 1, 1).setValues([['大分類']]).setFontWeight('bold').setBackground('#e8eaed');
  sh.getRange(2, 1, names.length, 1).setValues(names);
  sh.setFrozenRows(1);
}

function writeConfigSheet_(ss) {
  var sh = ensureSheet_(ss, SHEETS.CONFIG);
  sh.clearContents();
  sh.getRange(1, 1, CONFIG_ROWS.length, CONFIG_ROWS[0].length).setValues(CONFIG_ROWS);
  sh.getRange(1, 1, 1, CONFIG_ROWS[0].length).setFontWeight('bold').setBackground('#e8eaed');
  sh.autoResizeColumns(1, CONFIG_ROWS[0].length);
}

function stampConfig_(ss) {
  var sh = ss.getSheetByName(SHEETS.CONFIG);
  var f = sh.createTextFinder('最終セットアップ日時').findNext();
  if (f) sh.getRange(f.getRow(), 2).setValue(new Date());
}

/* ============================ 列の見出し解決 ============================ */

/** 見出しテキストの正規化（前後空白・内部空白の除去、全角括弧→半角）。 */
function normHeader_(s) {
  return String(s).trim().replace(/\s+/g, '').replace(/（/g, '(').replace(/）/g, ')');
}

/** 列番号(1始まり)→列レター（例: 28→'AB'）。 */
function colLetter_(n) {
  var s = '';
  while (n > 0) { var m = (n - 1) % 26; s = String.fromCharCode(65 + m) + s; n = Math.floor((n - m - 1) / 26); }
  return s;
}

/** 1行目の見出しから {正規化見出し: 列番号} を作る。 */
function headerIndexMap_(sh) {
  var lastCol = Math.max(sh.getLastColumn(), 1);
  var headers = sh.getRange(1, 1, 1, lastCol).getValues()[0];
  var map = {}, nonEmpty = 0;
  for (var i = 0; i < headers.length; i++) {
    var h = headers[i];
    if (String(h).trim() !== '') { nonEmpty++; var k = normHeader_(h); if (map[k] === undefined) map[k] = i + 1; }
  }
  return { map: map, nonEmpty: nonEmpty, lastCol: lastCol };
}

/**
 * ★メイン表の列位置を「見出し名」から解決する。
 *  - 見つかった見出しはその位置を使う（列を並び替えても追従）。
 *  - システム補助列(小分類候補文/小分類用結合文/要約用結合文)が無ければ末尾に新設。
 *  - 見出しが実質空のシートは、従来の固定位置(Config.gs)にフォールバック。
 *  - 必須見出しが見つからない場合はエラー（見出し名の確認を促す）。
 * @return {{COLX:Object, CLX:Object, dynamic:boolean}}
 */
function resolveColumns_(sh) {
  var hi = headerIndexMap_(sh);
  var rawKey = normHeader_(HEADER_TEXT.RAW);

  // 見出しがほぼ無い（新規/空シート）→ 固定位置にフォールバック
  if (hi.nonEmpty === 0 || (hi.map[rawKey] === undefined && hi.nonEmpty < 5)) {
    var COLX0 = {}, CLX0 = {};
    Object.keys(HEADER_TEXT).forEach(function (key) { COLX0[key] = COL[key]; CLX0[key] = colLetter_(COL[key]); });
    Logger.log('resolveColumns_: 見出し未検出のため固定位置を使用');
    return { COLX: COLX0, CLX: CLX0, dynamic: false };
  }

  var COLX = {}, CLX = {}, missing = [];
  var nextCol = hi.lastCol + 1;
  Object.keys(HEADER_TEXT).forEach(function (key) {
    var idx = hi.map[normHeader_(HEADER_TEXT[key])];
    if (idx === undefined) {
      if (SYSTEM_HELPER_KEYS.indexOf(key) >= 0) {
        sh.getRange(1, nextCol).setValue(HEADER_TEXT[key]).setFontWeight('bold').setBackground('#d9ead3');
        idx = nextCol; nextCol++;
        Logger.log('補助列を新設: ' + HEADER_TEXT[key] + '（列' + colLetter_(idx) + '）');
      } else {
        missing.push(HEADER_TEXT[key]);
        return;
      }
    }
    COLX[key] = idx; CLX[key] = colLetter_(idx);
  });

  if (missing.length) {
    throw new Error('メイン表「' + SHEETS.CLAIM + '」に必要な見出しが見つかりません。\n' +
      '1行目の見出し名を確認してください（並び順は自由ですが名称は一致が必要）:\n・' + missing.join('\n・'));
  }
  return { COLX: COLX, CLX: CLX, dynamic: true };
}

/** 固定位置(Config.gsのCOL/CL)から解決マップを作る（フォールバック用）。 */
function staticRes_() {
  var COLX = {}, CLX = {};
  Object.keys(HEADER_TEXT).forEach(function (k) { COLX[k] = COL[k]; CLX[k] = colLetter_(COL[k]); });
  return { COLX: COLX, CLX: CLX, dynamic: false };
}

/* ============================ メイン表 ============================ */

/** 数式ビルダーが参照する列レターマップ（実行時に resolveColumns_ の結果をセット）。 */
var ACTIVE_CL = null;

/** メイン表に、許可された数式列だけを設定する（AI列は原文行のみ）。 */
function setupMainSheet_(sh, res) {
  var COLX = res.COLX;
  ACTIVE_CL = res.CLX; // buildFormula_ がこの列レターで数式を作る

  var lastData = getLastDataRow_(sh, COLX.RAW);
  var templateLast = Math.max(lastData, LIMITS.TEMPLATE_ROWS + 1);

  FORMULA_TARGET_COLS.forEach(function (key) {
    if (AI_COLS.indexOf(key) >= 0) {
      if (lastData >= 2) setColumnFormulas_(sh, COLX[key], 2, lastData, buildFormula_.bind(null, key));
    } else {
      setColumnFormulas_(sh, COLX[key], 2, templateLast, buildFormula_.bind(null, key));
    }
  });

  ACTIVE_CL = null; // 後始末

  var msg;
  if (lastData >= 2) {
    msg = '数式を設定しました。AI列: 2〜' + lastData + '行 ／ 補助列: 2〜' + templateLast + '行'
        + (res.dynamic ? '（見出し名で列を自動解決）' : '（固定位置）') + '。';
  } else {
    msg = '補助列のテンプレ数式を 2〜' + templateLast + '行に入れました。'
        + 'AB(原文)を入力すると、その行が自動分類されます（onEdit）。';
  }
  SpreadsheetApp.getActive().toast(msg, 'クレームAI分類', 8);
  Logger.log('setupMainSheet_: ' + msg);
}

function setColumnFormulas_(sh, col, from, to, builder) {
  var n = to - from + 1;
  if (n <= 0) return;
  var arr = [];
  for (var r = from; r <= to; r++) arr.push([builder(r)]);
  sh.getRange(from, col, n, 1).setFormulas(arr);
}

function getLastDataRow_(sh, col) {
  var last = sh.getLastRow();
  if (last < 2) return 1;
  var vals = sh.getRange(2, col, last - 1, 1).getValues();
  for (var i = vals.length - 1; i >= 0; i--) {
    if (String(vals[i][0]).trim() !== '') return i + 2;
  }
  return 1;
}

/* ============================ 数式ビルダー ============================ */
/*
 * AI列は必ず =AI(単一セル) のみ。結合文は通常関数で作りAIの外側に置く。
 * 列参照は Config.gs の CL（列レター）を使用。
 */
function buildFormula_(key, r) {
  var AIF = AI_FUNCTION_NAME;
  var NL = 'CHAR(10)';

  // マスター範囲
  var D = SHEETS.RULE_DAI, Cn = SHEETS.RULE_CHU, Sn = SHEETS.RULE_SHO;
  var DAI_A="'"+D+"'!$A$2:$A$"+LIMITS.DAI, DAI_B="'"+D+"'!$B$2:$B$"+LIMITS.DAI,
      DAI_C="'"+D+"'!$C$2:$C$"+LIMITS.DAI, DAI_D="'"+D+"'!$D$2:$D$"+LIMITS.DAI,
      DAI_E="'"+D+"'!$E$2:$E$"+LIMITS.DAI;
  var CHU_A="'"+Cn+"'!$A$2:$A$"+LIMITS.CHU, CHU_B="'"+Cn+"'!$B$2:$B$"+LIMITS.CHU,
      CHU_C="'"+Cn+"'!$C$2:$C$"+LIMITS.CHU, CHU_D="'"+Cn+"'!$D$2:$D$"+LIMITS.CHU,
      CHU_E="'"+Cn+"'!$E$2:$E$"+LIMITS.CHU;
  var SHO_A="'"+Sn+"'!$A$2:$A$"+LIMITS.SHO, SHO_B="'"+Sn+"'!$B$2:$B$"+LIMITS.SHO,
      SHO_C="'"+Sn+"'!$C$2:$C$"+LIMITS.SHO, SHO_D="'"+Sn+"'!$D$2:$D$"+LIMITS.SHO,
      SHO_E="'"+Sn+"'!$E$2:$E$"+LIMITS.SHO;
  var SCN_A="'"+SHEETS.SCENE_M+"'!$A$2:$A$"+LIMITS.SCENE, SCN_B="'"+SHEETS.SCENE_M+"'!$B$2:$B$"+LIMITS.SCENE,
      SCN_R="'"+SHEETS.SCENE_RULE+"'!$A$2:$A$"+LIMITS.SCENE;
  var CAU_A="'"+SHEETS.CAUSE_M+"'!$A$2:$A$"+LIMITS.CAUSE, CAU_B="'"+SHEETS.CAUSE_M+"'!$B$2:$B$"+LIMITS.CAUSE,
      CAU_R="'"+SHEETS.CAUSE_RULE+"'!$A$2:$A$"+LIMITS.CAUSE;

  var L = ACTIVE_CL || CL; // 実行時に解決した列レター（無ければ固定既定）
  var AB=L.RAW, AD=L.AITEXT, AE=L.AI_DAI, AF=L.AI_CHU, AG=L.AI_SHO, AH=L.AI_SCENE,
      AP=L.G_DAI, AQ=L.I_CHU, AR=L.J_CHU, AS=L.L_REASON, AT=L.P_SCENE, AU=L.R_CAUSE,
      CJ=L.V_SHO, CK=L.W_SHO, CLc=L.SUM_TXT;

  switch (key) {

    case 'AITEXT': // AD 匿名化（カスタム関数に依存しない純粋数式＝スクリプト不具合の影響を受けない）
      return '=IF($'+AB+r+'="","",' +
        'REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(REGEXREPLACE(' +
        '$'+AB+r+',' +
        '"[A-Za-z0-9._%+\\-]+@[A-Za-z0-9.\\-]+\\.[A-Za-z]{2,}","[メール]"),' +
        '"https?://[^\\s　、。]+","[URL]"),' +
        '"〒?\\s?\\d{3}[-－]\\d{4}","[郵便番号]"),' +
        '"0\\d{1,4}[-－(（]?\\d{1,4}[-－)）]?\\d{3,4}","[電話番号]"),' +
        '"(会員番号|会員No|注文番号|受注番号|レシート番号|伝票番号)[ 　]*[:：]?[ 　]*[A-Za-z0-9\\-－]{3,}","$1：[番号]"),' +
        '"(氏名|お名前|お客様名|顧客名|名前)[ 　]*[:：][ 　]*[^ 　、。]{1,20}","$1：[氏名]"),' +
        '"(ご住所|住所|お届け先|届け先)[ 　]*[:：][ 　]*[^ 　、。]{1,40}","$1：[住所]"),' +
        '"\\d{7,}","[番号]"))';

    case 'SUM_TXT': // CL 要約用結合文（AC=AI要約の入力プロンプト）
      return '=IF($'+AD+r+'="","",' +
        '"あなたはお客様相談窓口の担当者です。次のご意見の要点だけを、お客様の立場で自然な文章に要約してください。次を厳守してください。(1)原文に書かれていない情報・推測・「適切に対応いたします」等の定型的な決まり文句を足さない。(2)文字数を埋めるための水増しをしない。内容が短ければ一文で構わない。(3)長くなる場合でも200文字以内（下限なし・短い方が良い）。(4)事実は変えない、箇条書きにしない、個人情報や固有の番号は含めない。"&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】要点のみの要約文。水増し禁止。最大200文字。")';

    case 'AI_SUM': // AC AI要約（200字・顧客目線）
      return '='+AIF+'($'+CLc+r+')';

    case 'G_DAI': // AP 大分類用結合文
      return '=IF($'+AD+r+'="","",' +
        '"あなたは食品スーパー・ドラッグストアのご意見分類担当です。次のご意見を読み、下記の大分類候補から最も適切なものを1つだけ選び、候補の表記どおりに分類名のみを出力してください。説明・記号・前置きは不要です。"&'+NL+'&' +
        '"【判定方針】お客様が最初に強く不満を持った主因で判定する。安易にその他へ寄せない。"&'+NL+'&' +
        '"【大分類候補と定義】"&'+NL+'&' +
        'TEXTJOIN('+NL+',TRUE,ARRAYFORMULA(IF('+DAI_A+'="","",'+DAI_A+'&"｜定義:"&'+DAI_B+'&"｜入る例:"&'+DAI_C+'&"｜しない例:"&'+DAI_D+'&"｜着眼点:"&'+DAI_E+')))&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】大分類名だけを1つ。■や「｜」等の記号・説明は付けない。")';

    case 'AI_DAI': // AE
      return '='+AIF+'($'+AP+r+')';

    case 'I_CHU': // AQ 中分類候補文（AI大分類でFILTER）
      return '=IF($'+AE+r+'="","",' +
        'TEXTJOIN('+NL+',TRUE,IFERROR(FILTER('+CHU_B+'&"｜定義:"&'+CHU_C+'&"｜入る例:"&'+CHU_D+'&"｜しない例:"&'+CHU_E+','+CHU_A+'=$'+AE+r+'),"")))';

    case 'J_CHU': // AR 中分類用結合文
      return '=IF($'+AQ+r+'="","",' +
        '"次のご意見について、指定された大分類に属する中分類候補から最も適切なものを1つだけ選び、候補の表記どおりに中分類名のみを出力してください。説明は不要です。"&'+NL+'&' +
        '"【大分類】"&$'+AE+r+'&'+NL+'&"【中分類候補】"&'+NL+'&$'+AQ+r+'&'+NL+'&' +
        '"【判定の注意】従業員個人の爪・髪・制服・名札・清潔感の問題は接客態度(身だしなみ)。店舗の清掃・衛生はクリンリネス。"&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】中分類名だけを1つ。■や「｜」等の記号・説明は付けない。")';

    case 'AI_CHU': // AF
      return '='+AIF+'($'+AR+r+')';

    case 'V_SHO': // CJ 小分類候補文（大分類×中分類でFILTER）
      return '=IF(OR($'+AE+r+'="",$'+AF+r+'=""),"",' +
        'TEXTJOIN('+NL+',TRUE,IFERROR(FILTER('+SHO_C+'&"｜定義:"&'+SHO_D+'&"｜入る例:"&'+SHO_E+','+SHO_A+'=$'+AE+r+','+SHO_B+'=$'+AF+r+'),"")))';

    case 'W_SHO': // CK 小分類用結合文
      return '=IF($'+CJ+r+'="","",' +
        '"次のご意見について、指定された大分類・中分類に属する小分類候補から最も適切なものを1つだけ選び、候補の表記どおりに小分類名のみを出力してください。該当が無ければ最も近いものを選ぶ。説明は不要です。"&'+NL+'&' +
        '"【大分類】"&$'+AE+r+'&"／【中分類】"&$'+AF+r+'&'+NL+'&"【小分類候補】"&'+NL+'&$'+CJ+r+'&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】小分類名だけを1つ。■や「｜」等の記号・説明は付けない。")';

    case 'AI_SHO': // AG
      return '='+AIF+'($'+CK+r+')';

    case 'L_REASON': // AS 補助セル（AI理由用）
      return '=IF($'+AD+r+'="","",' +
        '"次のご意見が、なぜ下記の分類になるのかを40文字以内で簡潔に説明してください。分類名の列挙ではなく理由を述べる。"&'+NL+'&' +
        '"【大分類】"&$'+AE+r+'&"／【中分類】"&$'+AF+r+'&"／【小分類】"&$'+AG+r+'&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】40文字以内の理由文。")';

    case 'AI_REASON': // AJ
      return '='+AIF+'($'+AS+r+')';

    case 'P_SCENE': // AT 発生場面候補文（結合文兼用）
      return '=IF($'+AD+r+'="","",' +
        '"次のご意見で、お客様の不満が最初に強くなった発生場面を、下記候補から1つだけ選び候補の表記どおりに場面名のみ出力してください。説明は不要です。"&'+NL+'&' +
        '"【発生場面候補】"&'+NL+'&TEXTJOIN('+NL+',TRUE,ARRAYFORMULA(IF('+SCN_A+'="","","・"&'+SCN_A+'&IF('+SCN_B+'=""," "," : "&'+SCN_B+'))))&'+NL+'&' +
        '"【判定ルール】"&TEXTJOIN(" ",TRUE,'+SCN_R+')&'+NL+'&' +
        '"【参考】大分類:"&$'+AE+r+'&"／中分類:"&$'+AF+r+'&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】場面名を1つだけ。")';

    case 'AI_SCENE': // AH
      return '='+AIF+'($'+AT+r+')';

    case 'R_CAUSE': // AU 原因分類候補文（結合文兼用）
      return '=IF($'+AD+r+'="","",' +
        '"次のご意見について、表面的な不満ではなく社内として直すべき背景要因を、下記候補から1つだけ選び候補の表記どおりに原因名のみ出力してください。説明は不要です。"&'+NL+'&' +
        '"【原因分類候補】"&'+NL+'&TEXTJOIN('+NL+',TRUE,ARRAYFORMULA(IF('+CAU_A+'="","","・"&'+CAU_A+'&IF('+CAU_B+'=""," "," : "&'+CAU_B+'))))&'+NL+'&' +
        '"【判定ルール】"&TEXTJOIN(" ",TRUE,'+CAU_R+')&'+NL+'&' +
        '"【参考】大分類:"&$'+AE+r+'&"／中分類:"&$'+AF+r+'&"／発生場面:"&$'+AH+r+'&'+NL+'&' +
        '"【ご意見内容】"&'+NL+'&$'+AD+r+'&'+NL+'&"【出力形式】原因名を1つだけ。")';

    case 'AI_CAUSE': // AI
      return '='+AIF+'($'+AU+r+')';

    default:
      throw new Error('未知の数式キー: ' + key);
  }
}

/* ============================ プルダウン ============================ */

function setupValidations_(ss, sh, res) {
  var COLX = res.COLX;
  var lastData = getLastDataRow_(sh, COLX.RAW);
  var last = Math.max(lastData, LIMITS.TEMPLATE_ROWS + 1); // 空行にもプルダウンを付ける
  var n = last - 1;

  // 基本（フルリスト）: 親未選択でも何か選べるように全件を入れておく
  applyListValidation_(sh, COLX.FIX_DAI,   2, n, rangeOfColumn_(ss, SHEETS.RULE_DAI, 1));
  applyListValidation_(sh, COLX.FIX_CHU,   2, n, rangeOfColumn_(ss, SHEETS.RULE_CHU, 2));
  applyListValidation_(sh, COLX.FIX_SHO,   2, n, rangeOfColumn_(ss, SHEETS.RULE_SHO, 3));
  applyListValidation_(sh, COLX.FIX_SCENE, 2, n, rangeOfColumn_(ss, SHEETS.SCENE_M, 1));
  applyListValidation_(sh, COLX.FIX_CAUSE, 2, n, rangeOfColumn_(ss, SHEETS.CAUSE_M, 1));

  // 既存行を「現在の確定大分類/中分類」で依存絞り込み（onEditに頼らず即反映）
  narrowExistingRows_(ss, sh, res, lastData);
  Logger.log('プルダウン設定完了（' + n + '行）');
}

/**
 * 既存の各行について、確定大分類(AK)で中分類(AL)候補を、
 * 確定中分類(AL)で小分類(AM)候補を、その行だけに絞り込む。
 */
function narrowExistingRows_(ss, sh, res, lastData) {
  if (lastData < 2) return;
  var COLX = res.COLX;
  var akv = sh.getRange(2, COLX.FIX_DAI, lastData - 1, 1).getValues();
  var alv = sh.getRange(2, COLX.FIX_CHU, lastData - 1, 1).getValues();
  for (var i = 0; i < akv.length; i++) {
    var row = i + 2;
    var dai = String(akv[i][0]).trim(), chu = String(alv[i][0]).trim();
    if (dai) setDependentValidation_(ss, sh, row, COLX.FIX_CHU, SHEETS.RULE_CHU, 1, 2, dai);
    if (dai && chu) setDependentTwoKeyValidation_(ss, sh, row, COLX.FIX_SHO, SHEETS.RULE_SHO, 1, 2, 3, dai, chu);
  }
}

function rangeOfColumn_(ss, sheetName, col) {
  var sh = ss.getSheetByName(sheetName);
  var last = getLastDataRow_(sh, col);
  if (last < 2) last = 2;
  return sh.getRange(2, col, last - 1, 1);
}

function applyListValidation_(sh, col, from, count, sourceRange) {
  var rule = SpreadsheetApp.newDataValidation()
    .requireValueInRange(sourceRange, true)
    .setAllowInvalid(true) // 表記揺れを手修正できるよう警告のみ
    .build();
  sh.getRange(from, col, count, 1).setDataValidation(rule);
}

/* ============================ 店番→店舗情報の補完 ============================ */

/** 店番を突合用に正規化（数字のみなら4桁ゼロ埋め。先頭ゼロの取りこぼしを防ぐ）。 */
function normStoreNo_(v) {
  var s = String(v == null ? '' : v).trim();
  if (s === '') return '';
  if (/^\d+$/.test(s)) return s.length < 4 ? ('0000' + s).slice(-4) : s;
  return s;
}

/**
 * 店舗マスタから {正規化店番: {見出し: 値, ...}} の対応表を作る。
 * 店舗マスタが無い/キー列が無い場合は null。
 */
function buildStoreLookup_(ss) {
  var st = ss.getSheetByName(SHEETS.STORE);
  if (!st) return null;
  var hi = headerIndexMap_(st);
  var keyCol = hi.map[normHeader_(STORE_KEY_HEADER)];
  if (!keyCol) return null;

  var srcCol = {};
  STORE_FILL_HEADERS.forEach(function (h) {
    var c = hi.map[normHeader_(h)];
    if (c) srcCol[h] = c;
  });

  var last = getLastDataRow_(st, keyCol);
  if (last < 2) return {};
  var data = st.getRange(2, 1, last - 1, st.getLastColumn()).getValues();
  var map = {};
  for (var i = 0; i < data.length; i++) {
    var no = normStoreNo_(data[i][keyCol - 1]);
    if (no === '') continue;
    var rec = {};
    STORE_FILL_HEADERS.forEach(function (h) { if (srcCol[h]) rec[h] = data[i][srcCol[h] - 1]; });
    map[no] = rec;
  }
  return map;
}

/**
 * 受付表の from..to 行について、店番から店舗情報(値)を書き込む。
 *  - 店番が空/店舗マスタに該当なしの行は既存値を維持（触れない）。
 *  - dest列は見出しで解決するので列の並び替えに強い。列単位のバッチ書き込みで高速。
 */
function fillStoreInfoRows_(ss, sh, from, to) {
  if (to < from) return;
  var lookup = buildStoreLookup_(ss);
  if (!lookup) return; // 店舗マスタ無し → 何もしない（分類機能は止めない）

  var hi = headerIndexMap_(sh);
  var keyCol = hi.map[normHeader_(STORE_KEY_HEADER)];
  if (!keyCol) return;

  var n = to - from + 1;
  var keys = sh.getRange(from, keyCol, n, 1).getValues();

  STORE_FILL_HEADERS.forEach(function (h) {
    var col = hi.map[normHeader_(h)];
    if (!col) return; // 受付表に該当見出しが無ければスキップ
    var cur = sh.getRange(from, col, n, 1).getValues(); // 既存値を保持
    var changed = false;
    for (var i = 0; i < n; i++) {
      var no = normStoreNo_(keys[i][0]);
      if (no === '') continue;
      var rec = lookup[no];
      if (!rec) continue;
      var v = rec[h];
      if (v !== undefined && v !== '' && cur[i][0] !== v) { cur[i][0] = v; changed = true; }
    }
    if (changed) sh.getRange(from, col, n, 1).setValues(cur);
  });
}

/** 全データ行の店舗情報を店番から一括補完（インポート後などに使用）。 */
function fillAllStoreInfo() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var ui = SpreadsheetApp.getUi();
  var sh = ss.getSheetByName(SHEETS.CLAIM);
  if (!sh) { ui.alert('メイン表「' + SHEETS.CLAIM + '」が見つかりません。'); return; }
  if (!ss.getSheetByName(SHEETS.STORE)) { ui.alert('「' + SHEETS.STORE + '」シートが見つかりません。'); return; }
  var hi = headerIndexMap_(sh);
  var keyCol = hi.map[normHeader_(STORE_KEY_HEADER)];
  if (!keyCol) { ui.alert('受付表に「' + STORE_KEY_HEADER + '」列（見出し）が見つかりません。'); return; }
  var last = getLastDataRow_(sh, keyCol);
  if (last < 2) { SpreadsheetApp.getActive().toast('店番が入力された行がありません。', 'クレームAI分類', 5); return; }
  fillStoreInfoRows_(ss, sh, 2, last);
  SpreadsheetApp.getActive().toast('店舗情報を補完しました（' + (last - 1) + '行）。', 'クレームAI分類', 5);
}

/* ============================ 精度検証 ============================ */

function setupAccuracySheet_(ss) {
  var sh = ensureSheet_(ss, SHEETS.ACCURACY);
  sh.clearContents();
  var C = "'" + SHEETS.CLAIM + "'";
  var R = LIMITS.ACC;

  // メイン表の列レターを見出しから解決（無ければ固定既定にフォールバック）
  var LET = {}; // key -> 列レター
  Object.keys(HEADER_TEXT).forEach(function (k) { LET[k] = colLetter_(COL[k]); });
  var storeLetter = REF_FALLBACK_LETTER.STORE;
  var claim = ss.getSheetByName(SHEETS.CLAIM);
  if (claim) {
    try {
      var hi = headerIndexMap_(claim);
      Object.keys(HEADER_TEXT).forEach(function (k) {
        var idx = hi.map[normHeader_(HEADER_TEXT[k])];
        if (idx) LET[k] = colLetter_(idx);
      });
      var sIdx = hi.map[normHeader_(REF_HEADER_TEXT.STORE)];
      if (sIdx) storeLetter = colLetter_(sIdx);
    } catch (e) { Logger.log('精度検証: 見出し解決失敗のため固定位置を使用 ' + e); }
  }
  function col(letter){ return C + '!$' + letter + '$2:$' + letter + '$' + R; }

  var AE=col(LET.AI_DAI),AK=col(LET.FIX_DAI), AF=col(LET.AI_CHU),AL=col(LET.FIX_CHU),
      AG=col(LET.AI_SHO),AM=col(LET.FIX_SHO), AH=col(LET.AI_SCENE),AN=col(LET.FIX_SCENE),
      AI=col(LET.AI_CAUSE),AO=col(LET.FIX_CAUSE), W=col(storeLetter), AC=col(LET.AI_SUM);

  var rows = [];
  rows.push(['クレーム(ご意見)AI分類 精度検証', '', '', '']);
  rows.push(['', '', '', '']);
  rows.push(['分類軸', '確定済み件数', '一致件数', '一致率']);

  function accRow(label, ai, fix) {
    return [label,
      '=SUMPRODUCT(('+fix+'<>"")*1)',
      '=SUMPRODUCT(('+ai+'='+fix+')*('+fix+'<>""))',
      '=IFERROR(C{ROW}/B{ROW},"-")'];
  }
  rows.push(accRow('大分類', AE, AK));
  rows.push(accRow('中分類', AF, AL));
  rows.push(accRow('小分類', AG, AM));
  rows.push(accRow('発生場面', AH, AN));
  rows.push(accRow('原因分類', AI, AO));

  rows.push(['', '', '', '']);
  rows.push(['偏りチェック（AI大分類）', 'AI件数', '全体に対する割合', '']);
  rows.push(['レジ接客への偏り',   '=COUNTIF('+AE+',"レジ接客")',   '=IFERROR(B{ROW}/COUNTA('+AE+'),"-")', '']);
  rows.push(['外周り接客への偏り', '=COUNTIF('+AE+',"外周り接客")', '=IFERROR(B{ROW}/COUNTA('+AE+'),"-")', '']);
  rows.push(['その他への偏り',     '=COUNTIF('+AE+',"その他")',     '=IFERROR(B{ROW}/COUNTA('+AE+'),"-")', '']);

  var values = rows.map(function (row, i) {
    var rowNo = 1 + i;
    return row.map(function (cell) {
      return (typeof cell === 'string') ? cell.replace(/\{ROW\}/g, rowNo) : cell;
    });
  });
  sh.getRange(1, 1, values.length, 4).setValues(values);

  sh.getRange(1, 1).setFontWeight('bold').setFontSize(14);
  sh.getRange(3, 1, 1, 4).setFontWeight('bold').setBackground('#e8eaed');
  sh.getRange(4, 4, 5, 1).setNumberFormat('0.0%');
  sh.getRange(11, 3, 3, 1).setNumberFormat('0.0%');

  // 誤分類一覧（大分類）: 左 A〜D
  var top = values.length + 3;
  sh.getRange(top, 1).setValue('■ 大分類 誤分類一覧（確定済みで AI≠確定）').setFontWeight('bold');
  sh.getRange(top + 1, 1, 1, 4).setValues([['店名', '内容(要約)', 'AI大分類', '確定大分類']])
    .setFontWeight('bold').setBackground('#f4cccc');
  sh.getRange(top + 2, 1).setFormula(
    '=IFERROR(FILTER({'+W+','+AC+','+AE+','+AK+'},('+AK+'<>"")*('+AE+'<>'+AK+')),"（不一致なし）")');

  // 誤分類一覧（中分類）: 右 F〜I（縦スピル衝突回避）
  sh.getRange(top, 6).setValue('■ 中分類 誤分類一覧（確定済みで AI≠確定）').setFontWeight('bold');
  sh.getRange(top + 1, 6, 1, 4).setValues([['店名', '内容(要約)', 'AI中分類', '確定中分類']])
    .setFontWeight('bold').setBackground('#f4cccc');
  sh.getRange(top + 2, 6).setFormula(
    '=IFERROR(FILTER({'+W+','+AC+','+AF+','+AL+'},('+AL+'<>"")*('+AF+'<>'+AL+')),"（不一致なし）")');

  sh.setColumnWidth(2, 320); sh.setColumnWidth(7, 320);
  Logger.log('精度検証シート構築完了');
}

/* ==================== 4/4 Code ==================== */
/**
 * Code.gs
 * ------------------------------------------------------------------
 * メニュー登録・イベント・テスト用ユーティリティ。
 * 実処理は Setup.gs / Anonymize.gs、設定は Config.gs 参照。
 * ------------------------------------------------------------------
 */

/** スプレッドシートを開いたときにメニューを追加。 */
function onOpen() {
  SpreadsheetApp.getUi()
    .createMenu('クレームAI分類')
    .addItem('① マスター系シートを作成/更新', 'setupMasters')
    .addItem('② メイン表に数式・プルダウンを適用', 'applyMainSheetFormulas')
    .addSeparator()
    .addItem('確定プルダウンを絞り込み直す（全行）', 'refreshDependentDropdowns')
    .addItem('店番から店舗情報を一括補完（全行）', 'fillAllStoreInfo')
    .addSeparator()
    .addItem('テストデータを投入（5ケース）', 'insertTestCases')
    .addItem('変更内容（計画）を表示', 'showSetupPlan')
    .addItem('マスターを既定値で再作成（上書き注意）', 'resetMastersToDefault')
    .addToUi();
}

function showSetupPlan() {
  SpreadsheetApp.getUi().alert(SETUP_PLAN.join('\n'));
}

/**
 * 全行の確定プルダウンを、現在の確定大分類/中分類にあわせて依存絞り込みし直す。
 * 一括貼り付けなどで onEdit が走らなかった場合の手動リフレッシュ用。
 */
function refreshDependentDropdowns() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sh = ss.getSheetByName(SHEETS.CLAIM);
  if (!sh) { SpreadsheetApp.getUi().alert('メイン表「' + SHEETS.CLAIM + '」が見つかりません。'); return; }
  var res;
  try { res = resolveColumns_(sh); } catch (e) { res = staticRes_(); }
  var lastData = getLastDataRow_(sh, res.COLX.RAW);
  narrowExistingRows_(ss, sh, res, lastData);
  SpreadsheetApp.getActive().toast('確定プルダウンを絞り込み直しました（' + Math.max(lastData - 1, 0) + '行）。', 'クレームAI分類', 5);
}

/**
 * メイン表の編集トリガ（自動化の要）。
 *  1) AB(原文)を入力/貼付 → その行に全数式を自動反映（AI分類が自動で走る）
 *  2) AK(確定大分類)を選ぶ → AL(確定中分類)候補を絞る
 *  3) AL(確定中分類)を選ぶ → AM(確定小分類)候補を絞る
 * 簡易トリガのため追加の権限設定は不要。失敗しても運用は止まらない。
 */
function onEdit(e) {
  try {
    if (!e || !e.range) return;
    var sh = e.range.getSheet();
    if (sh.getName() !== SHEETS.CLAIM) return;
    var ss = e.source;
    var r1 = e.range.getRow(), r2 = e.range.getLastRow();
    var c1 = e.range.getColumn(), c2 = e.range.getLastColumn();

    // 見出しから列解決。失敗しても固定位置で続行（プルダウン絞り込みを止めない）
    var res;
    try { res = resolveColumns_(sh); } catch (e2) { res = staticRes_(); }
    var COLX = res.COLX;

    // 1) 原文(内容（原文）)が編集範囲に含まれる → 対象行に数式を自動反映
    if (c1 <= COLX.RAW && COLX.RAW <= c2) {
      autoFillRows_(sh, Math.max(r1, 2), r2, res);
    }

    // 1.5) 店番が編集範囲に含まれる → 店舗情報(店名/営業統括部/営業部/ブロック/エリア)を補完
    try {
      var keyColS = headerIndexMap_(sh).map[normHeader_(STORE_KEY_HEADER)];
      if (keyColS && c1 <= keyColS && keyColS <= c2) {
        fillStoreInfoRows_(ss, sh, Math.max(r1, 2), r2);
      }
    } catch (e3) { Logger.log('店舗補完: ' + e3); }

    // 2)(3) 確定プルダウンの依存絞り込み（単一セル編集時のみ）
    if (e.range.getNumRows() === 1 && e.range.getNumColumns() === 1 && r1 >= 2) {
      if (c1 === COLX.FIX_DAI) {
        setDependentValidation_(ss, sh, r1, COLX.FIX_CHU, SHEETS.RULE_CHU, 1, 2, e.value);
        sh.getRange(r1, COLX.FIX_SHO).clearDataValidations();
      }
      if (c1 === COLX.FIX_CHU) {
        var dai = sh.getRange(r1, COLX.FIX_DAI).getValue();
        setDependentTwoKeyValidation_(ss, sh, r1, COLX.FIX_SHO, SHEETS.RULE_SHO, 1, 2, 3, dai, e.value);
      }
    }
  } catch (err) {
    Logger.log('onEdit: ' + err);
  }
}

/**
 * 指定行範囲のうち、原文が入っている行に全数式を設定する。
 * onEdit から呼ばれ、原文入力だけで自動分類されるようにする。
 */
function autoFillRows_(sh, from, to, res) {
  var COLX = res.COLX;
  ACTIVE_CL = res.CLX; // buildFormula_ が解決済み列レターで数式を作る
  try {
    for (var r = from; r <= to; r++) {
      var ab = sh.getRange(r, COLX.RAW).getValue();
      if (String(ab).trim() === '') continue; // 空行はスキップ（AI("")防止）
      FORMULA_TARGET_COLS.forEach(function (key) {
        sh.getRange(r, COLX[key]).setFormula(buildFormula_(key, r));
      });
    }
  } finally {
    ACTIVE_CL = null;
  }
}

function setDependentValidation_(ss, sh, row, targetCol, masterName, keyCol, valCol, keyVal) {
  var cell = sh.getRange(row, targetCol);
  if (!keyVal) { cell.clearDataValidations(); return; }
  var m = ss.getSheetByName(masterName);
  var last = getLastDataRow_(m, valCol);
  if (last < 2) return;
  var data = m.getRange(2, 1, last - 1, Math.max(keyCol, valCol)).getValues();
  var list = uniq_(data.filter(function (r) { return r[keyCol - 1] === keyVal; })
                       .map(function (r) { return r[valCol - 1]; }));
  applyExplicitList_(cell, list);
}

function setDependentTwoKeyValidation_(ss, sh, row, targetCol, masterName, k1Col, k2Col, valCol, k1, k2) {
  var cell = sh.getRange(row, targetCol);
  if (!k1 || !k2) { cell.clearDataValidations(); return; }
  var m = ss.getSheetByName(masterName);
  var last = getLastDataRow_(m, valCol);
  if (last < 2) return;
  var data = m.getRange(2, 1, last - 1, Math.max(k1Col, k2Col, valCol)).getValues();
  var list = uniq_(data.filter(function (r) { return r[k1Col - 1] === k1 && r[k2Col - 1] === k2; })
                       .map(function (r) { return r[valCol - 1]; }));
  applyExplicitList_(cell, list);
}

function uniq_(arr) {
  var seen = {}, out = [];
  arr.forEach(function (v) { if (v !== '' && !seen[v]) { seen[v] = 1; out.push(v); } });
  return out;
}

function applyExplicitList_(cell, list) {
  if (!list || !list.length) { cell.clearDataValidations(); return; }
  var rule = SpreadsheetApp.newDataValidation()
    .requireValueInList(list, true).setAllowInvalid(true).build();
  cell.setDataValidation(rule);
}

/**
 * テストケースをメイン表の末尾に投入する（AB=原文, A=状況に"TEST"の目印）。
 * 投入後にメイン表の数式・プルダウンを適用する。※テスト行は後で削除可。
 */
function insertTestCases() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sh = ss.getSheetByName(SHEETS.CLAIM);
  if (!sh) { SpreadsheetApp.getUi().alert('メイン表「' + SHEETS.CLAIM + '」が見つかりません。'); return; }
  if (!ss.getSheetByName(SHEETS.RULE_CHU)) setupMasters();

  var cases = [
    'レジ担当者のネイルが長く、食品を扱う店として衛生面が気になった',                 // レジ接客/接客態度/身だしなみ
    'チラシの特売品を買おうとしたが棚になく、入荷予定も分からなかった',               // 欠品・特注/欠品/チラシ
    '棚の価格は198円だったが、レジでは248円になっていた',                           // アンマッチ/売価アンマッチ
    '売場で在庫を尋ねたのに、店員が調べてくれず対応が悪かった',                       // 外周り接客/売場・作業時/在庫確認・問い合わせ対応
    '返品できると言われたが、後日別の担当者から返品できないと言われた'                // 外周り接客/売場・作業時/返品・交換対応
  ];

  var res = resolveColumns_(sh); // 見出し名から列位置を解決
  var COLX = res.COLX;
  var last = getLastDataRow_(sh, COLX.RAW);
  var start = Math.max(last + 1, 2);
  // A列(状況)に目印、原文列に本文
  var marks = cases.map(function () { return ['TEST']; });
  var texts = cases.map(function (t) { return [t]; });
  sh.getRange(start, 1, marks.length, 1).setValues(marks);
  sh.getRange(start, COLX.RAW, texts.length, 1).setValues(texts);

  setupMainSheet_(sh, res);
  setupValidations_(ss, sh, res);
  SpreadsheetApp.getActive().toast('テスト5件を投入し数式を適用しました（A列=TEST）。', 'クレームAI分類', 6);
}
